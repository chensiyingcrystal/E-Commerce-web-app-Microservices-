import { CustomError } from "./custom-error";
export declare class NotAuthroizedError extends CustomError {
    statusCode: number;
    constructor();
    serializeErrors(): {
        message: string;
    }[];
}
